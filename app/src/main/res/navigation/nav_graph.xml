<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MainFragment">
    <fragment
        android:id="@+id/MainFragment"
        android:name="com.example.githubtest.presenter.home.main.MainFragment"
        android:label="UserFragment"
        tools:layout="@layout/fragment_main">

        <action
            android:id="@+id/action_user_framgent_favorite_fragment"
            app:destination="@id/FavoriteFragment" />
    </fragment>

    <fragment
        android:id="@+id/UserFragment"
        android:name="com.example.githubtest.presenter.home.user.UserFragment"
        android:label="UserFragment"
        tools:layout="@layout/fragment_user">

    </fragment>
    <fragment
        android:id="@+id/ReposFragment"
        android:name="com.example.githubtest.presenter.home.repos.ReposFragment"
        android:label="ReposFragment"
        tools:layout="@layout/fragment_repos">

    </fragment>
    <fragment
        android:id="@+id/FavoriteFragment"
        android:name="com.example.githubtest.presenter.favorites.FavoriteFragment"
        android:label="FavoriteFragment"
        tools:layout="@layout/favorite_fragment">

    </fragment>
</navigation>